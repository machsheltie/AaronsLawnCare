import ServicePageTemplate from './ServicePageTemplate';

export default function SpringCleanupPage() {
  return (
    <ServicePageTemplate
      serviceName="Spring Cleanup Services"
      description="Comprehensive spring cleanup to prepare your lawn for the growing season. We remove winter debris, refresh mulch, and get your property ready for spring."
    />
  );
}
